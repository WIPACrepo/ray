{{- if .Values.ingress.issuer.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  annotations:
  name: {{ .Release.Name }}-cert
spec:
  commonName: i3ray.nrp-nautilus.io
  dnsNames:
  - i3ray.nrp-nautilus.io
  issuerRef:
    kind: Issuer
    name: letsencrypt
  secretName: {{ .Release.Name }}-tls
{{- end }}
